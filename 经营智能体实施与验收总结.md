# 经营智能体实施与验收总结

更新时间：2026-07-25

## 结论

已完成一个可运行、可持久化、具备权限边界的“经营/生产智能体简版”闭环。它面向**已授权的 API、Webhook、系统导出或企业机器人**接入，不抓取个人微信或普通微信群历史消息，不绕过登录，不逆向接口。

当前版本可以作为客户系统对接前的本地/测试环境最小可用版本；不能诚实地称为“已接通客户 OA、小程序、企业微信和真实大模型的生产系统”。这些外部依赖仍须按客户的授权、接口和部署环境逐项完成验收。

## 已完成的内容

### 1. 三类业务智能体与权限隔离

| 角色/智能体 | 已实现行为 | 服务端保护 |
| --- | --- | --- |
| 老板智能体 | 汇总本工作区已授权经营数据、预警、日报与老板任务；支持私聊问答 | 仅工作区所有者可读写；成员、同工作区管理员和平台管理员不能通过通用接口读取老板私聊 |
| 私事员工智能体 | 处理老板私事范围的数据与对话，不混入公司公事数据 | 仅当前所有者可读写；私有数据存在时禁止静默转移所有权 |
| 公事员工智能体 | 公司成员查询本工作区已授权的 OA、小程序、生产日报等公事数据，并进行协作问答 | 按工作区隔离；成员不能读取老板或私事范围的数据 |

当前助手回答基于数据库内已授权记录生成**确定性摘要与来源线索**，不会伪造“已调用外部模型”的结果。

### 2. 已授权数据接入与可追溯性

- 可登记公司 OA、公司小程序、生产日报、企业机器人/群聊、自有业务接口等数据源。
- 支持受控 API/Webhook 写入；创建或轮换时一次性返回接入令牌，数据库只保存令牌摘要。
- 支持认证后的管理端录入和受控导入路径；不向外部写入令牌开放历史数据读取能力。
- 每条业务记录保存数据源、外部标识、发生时间、采集批次和范围；同一数据源的 `external_id` 可幂等重试。
- 重复外部写入只返回最小回执，不回显既有记录的正文、负载或私有内容。

### 3. 预警、日报和老板任务

- 已提供生产异常、订单风险、设备故障、交期风险、审批超时五类默认中文规则。
- 新入站记录命中规则后自动建立可追溯预警；成员可确认收到，只有工作区所有者或管理员可以结案。
- 生产日报按工作区与日期幂等生成，只汇总公司范围的已授权数据，明确排除老板/私事范围记录。
- 老板可下达任务、指定负责人并跟踪处理进度；负责人仅能更新自己被分配任务的状态和进度。
- 用户端工作台展示数据源、预警、日报和任务概览，所有关键业务操作记录在审计链路中。

### 4. 前端与模型运营边界

- 用户端：React + Ant Design + `@ant-design/x`，新增“经营助手”页面，包含三助手切换、聊天、数据源登记、预警、日报和任务流程。
- 管理员后台：React + Ant Design，中文模型中心、审计、用户、工作区、MCP、权限策略和运行设置入口均已保留。
- LiteLLM：作为服务端统一模型调用/路由能力保留，模型运营收拢到管理员后台；默认不再额外暴露第三个 LiteLLM 控制台，也不在浏览器或文档中暴露供应商密钥。

### 5. 文档与安全措施

- README 已说明本地访问地址、默认本地开发凭据、LiteLLM 合并策略、接口目录与合规边界。
- README 已增加 Windows Docker IPv4 转发异常时使用 IPv6 回环地址的说明。
- OpenAPI 接口目录已中文化，经营智能体接口可供 OA、小程序、中间件或企业机器人按授权接入。
- 私有审计元数据按所有者隔离；平台管理员和同工作区管理员不能借审计接口读取老板/私事内容。
- 数据源令牌、哈希和真实供应商密钥不会出现在列表、OpenAPI、审计或 README 中。

## 已完成的验证

| 验证项 | 结果 |
| --- | --- |
| Python 全量回归 | `32/32` 通过，覆盖迁移、授权、工作区隔离、幂等、预警、日报、任务、私有审计和中文接口目录 |
| 真实 PostgreSQL HTTP 验收 | `54/54` 通过，覆盖老板/成员/管理员权限、私聊隔离、令牌轮换与脱敏、错误令牌、幂等、预警、日报隐私过滤、任务进度、所有权保护和停用数据源 |
| 数据库迁移 | PostgreSQL 当前版本为 `20260725_03` |
| API 就绪检查 | `http://[::1]:8000/api/v1/health/ready` 返回 `200` |
| Docker 服务 | API、PostgreSQL、MCP、用户端、管理员端均处于运行状态；API 与 PostgreSQL 健康检查正常 |
| 用户端生产构建 | 通过；经营助手分包已产出 |
| 管理员端生产构建 | 通过 |
| 页面交互验收 | 已实际进入用户端“经营助手”，刷新数据、切换公事员工智能体、打开并关闭数据源登记表单；已实际进入管理员后台“模型中心”并确认中文状态呈现 |
| 差异与部署配置检查 | `git diff --check` 与 `docker compose config --quiet` 通过 |

## 当前可用的本地访问方式

正常情况下可按 README 使用 `localhost` 地址。当前 Windows/Docker 环境的 IPv4 转发存在本机问题，`127.0.0.1` Docker 映射可能返回 `502`；本次已实际验证以下 IPv6 回环地址可用：

| 服务 | 已验证地址 |
| --- | --- |
| API | `http://[::1]:8000` |
| 用户端 | `http://[::1]:8081` |
| 管理员后台 | `http://[::1]:8082` |

这属于本机 Docker Desktop/WSL 端口转发问题，不是容器内 API 或数据库故障。生产部署应使用受控域名、TLS 与反向代理，不应直接暴露这些本机端口。

## 尚未完成、必须在客户上线前完成的工作

### A. 客户真实系统接入（必须由客户授权）

1. 明确公司小程序类型、登录方式、开放 API、回调签名、字段字典和数据更新频率。
2. 与 OA、ERP、MES、生产日报等系统的负责人确认最小权限账号、只读范围、令牌轮换、IP 白名单和错误重试策略。
3. 如需微信渠道，只接入企业微信、微信官方机器人、官方回调或客户明确授权的中间件；落实员工告知、群范围、保存期限和删除机制。
4. 为每个客户数据源配置独立的密钥管理、环境变量或密钥管理系统，不能把真实密钥放进浏览器、README 或代码仓库。

### B. 通知与自动化运营（当前未接通外部渠道）

1. 当前老板端可在工作台看到预警、日报和任务；尚未配置真实的企业微信/短信/邮件/钉钉等**外部主动推送通道**。
2. 如需固定时段日报、实时预警、预警分级升级、周报/月报，需要增加受控调度器、通知订阅配置、失败重试与送达审计。
3. 外部通知应使用客户提供的官方机器人或受控 Webhook，并在上线前做签名校验、超时、重试、限流和告警测试。

### C. 真实 AI 模型验收

1. 当前 LiteLLM 上游真实模型响应尚未在本机得到供应商级验证；管理员后台会明确标记未配置/不可调用的模型，不会假装可用。
2. 生产前需在“模型中心”配置真实供应商凭据（只经部署环境/密钥管理读取），发起真实探测并记录模型、版本、超时、失败策略与成本上限。
3. 经营数据用于模型提示前，还需要按客户制度完成脱敏、提示词边界、人工复核和输出留存策略。

### D. 生产化基础设施

1. 替换所有本地默认管理员、数据库和 JWT 凭据；启用 TLS、受限 CORS、备份与灾难恢复演练。
2. 将附件和长期业务数据迁移到受控对象存储，配置生命周期、恶意文件扫描、备份和访问审计。
3. 接入集中日志、指标、错误追踪、容量监控、告警和值班流程；建立数据库迁移发布与回滚演练。
4. 用客户真实但脱敏的数据做 UAT，覆盖异常数据、重复回调、网络超时、权限变更、负责人离职、数据源停用和审计追溯。

## 推荐的客户实施顺序

1. 选定一个只读生产日报或 OA 查询接口作为首个数据源，完成字段映射和最小权限授权。
2. 在测试工作区通过 API/Webhook 写入真实结构的脱敏样本，确认预警规则、日报口径和老板任务流程。
3. 由老板与一名员工分别验收老板私聊、公事协作、预警确认和任务进度更新。
4. 接入一个官方企业消息通道用于“预警/日报待办”通知，并做送达与失败重试验收。
5. 配置并探测一个真实模型供应商，再决定哪些摘要由确定性规则完成、哪些问题允许 AI 辅助回答。
6. 完成安全、备份、监控、容量和 UAT 后，再进入生产发布。

## 关键文件

- `api/business_routes.py`：经营智能体、数据源、入站、预警、日报、任务与权限接口。
- `db/models.py`：业务助手、数据源、业务记录、预警、日报、任务与私有消息模型。
- `migrations/versions/20260725_03_business_agent.py`：数据库迁移。
- `frontend/src/components/BusinessAssistantsPage.jsx`：用户端经营助手页面（React + `@ant-design/x`）。
- `api/openapi.py`：中文接口目录。
- `tests/test_business_api.py`：经营智能体接口与权限回归测试。
- `README.md`：部署、账号、地址、接口接入与合规说明。

## 诚实的交付边界

本项目现在具备“可接、可管、可审计、可隔离、可验证”的简版业务底座；它不是对 WorkBuddy、企业微信、OA、小程序或任何模型供应商的 100% 现成替代。只有在客户真实接口、账号授权、消息渠道和模型供应商均逐项验收后，才能称为该客户环境中的可商用版本。
